// Template for config.js — copy this file to config.js and fill in your
// own Supabase project's values (Settings → API in your Supabase dashboard).
//
//   cp config.example.js config.js
//
// config.js is git-ignored, so your real keys never get committed. Without
// a config.js at all, the app just runs fully local with no cloud sync —
// nothing breaks.
window.MISTEX_SUPABASE_URL = "https://xxxxxxxx.supabase.co";
window.MISTEX_SUPABASE_ANON_KEY = "your-anon-public-key-here";
